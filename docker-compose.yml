version: '3.7'
services:

  graph-processing_text_gene_match:
    build: .
    environment:
      - GC_NEO4J_URL=bolt://host.docker.internal:7687
      - GC_NEO4J_USER=neo4j
      - GC_NEO4J_PASSWORD=test
